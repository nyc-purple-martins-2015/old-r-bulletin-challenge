<div class="new-message-link">
  <% if log_in? %>
    <%= link_to "New Message", "/conversations/#{@conversation.id}/messages/new" %>
  <% end %>
</div>
<div class="message-list">
  <% @conversation.messages.each do |message| %>
  <p><%=message.content %></p>
  <% end %>
</div>
