<div class="new-conversation">
  <%= form_for @conversation do |f| %>
  <p>
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </p>

  <p>
    <%= f.collection_select :topic_id, @topics, :id, :name, prompt: true %>
  </p>

  <p>
    <%= f.submit %>
  </p>
  <% end %>
</div>
